architecture: x86_64
service_name: Pizza Ebingen
type: web
description: order some pizza online
flag_id_description: flag_id is the name used when ordering"
apt_dependencies:
    - build-essential
    - g++-8
    - cmake
    - libssl-dev
    - libsqlite3-dev
    - sqlite3 
    - libboost1.67-dev # requires custom ppa:mhier/libboost-latest